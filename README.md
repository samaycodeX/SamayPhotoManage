# SamayPhotoManage

Event photography studio management system. Copy `server/.env.example` to `server/.env`, configure MongoDB and run `npm.cmd install`, then `npm.cmd run dev`.

The first admin account is created automatically from `ADMIN_EMAIL`, `ADMIN_PASSWORD`, and `FINANCE_PIN` on server startup.
