{
  "name": "samay-photo-manage",
  "private": true,
  "workspaces": ["server", "client"],
  "scripts": {"dev": "concurrently \"npm --workspace server run dev\" \"npm --workspace client run dev\"", "start": "npm --workspace server start", "build": "npm --workspace client run build"},
  "devDependencies": {"concurrently": "^9.1.2"}
}
